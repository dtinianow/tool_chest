<%= render partial: 'shared/summary' %>

<h1>Edit Tool</h1>
<%= form_for([@user, @tool]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :quantity %>
  <%= f.number_field :quantity %>

  <%= f.label :price %>
  <%= f.text_field :price %>

  <%= f.submit %>
<% end %>
